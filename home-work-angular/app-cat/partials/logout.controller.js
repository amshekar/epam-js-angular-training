angular.module('catapp')
    .controller('LogoutCtrl', function ($location,  toastr) {
        //if (!$auth.isAuthenticated()) { return; }
        //$auth.logout()
        //    .then(function () {
        //        toastr.info('You have been logged out');
        //        $location.path('/');
        //    });
    });